export class CheckBox {
    async expendOptions(): Promise<void> {
    }
    async checkAll(){
        //await this.page.locator(this.subDirectoryBox)
        //.first()
       // .check()
        //await this.page.locator('input[type=checkbox').first().check()
    }
}