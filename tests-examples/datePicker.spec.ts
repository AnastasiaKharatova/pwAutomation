import {test as it} from '@playwright/test';
import {DatePicker} from '../../playwrightAutomationProject/page_object/DatePicker';
it.describe('DATE PICKER', () => {
   it('working with date picker', async({page}) => {
    //let datePicker()
   })
});